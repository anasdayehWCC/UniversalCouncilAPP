#!/usr/bin/env bash
set -euo pipefail

echo "[apply-openapi-patches] Legacy no-op."
echo "[apply-openapi-patches] OpenAPI generation is now owned by universal-app via 'pnpm openapi:web'."
echo "[apply-openapi-patches] If the generated client is missing fields, fix the backend schema/spec or update universal-app/src/lib/api/generated directly."
